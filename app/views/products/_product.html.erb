<tr>
  <td><%= product.brand.name %></td>
  <td><%= product.name %></td>
  <td><%= product.price %></td>
  <td class="positive"><%= link_to 'Show', [@brand, product] %></td>
  <td class="warning">
    <%#= link_to 'Edit', edit_brand_product_path(@brand, product) %>
    <%= link_to 'Edit', [:edit, @brand, product] %>
  </td>
  <td class="negative">
    <%#= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
    <%= link_to semantic_icon('trash')+'Destroy',[@brand, product], method: :delete, data: { confirm: 'Are you sure?' } %>
  </td>
</tr>